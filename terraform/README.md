This Terraform config manages the backtracc.tech DNS records and domain on Cloudflare.

Ensure to have the following environment variables set before working with this setup:

- `CLOUDFLARE_API_TOKEN`
